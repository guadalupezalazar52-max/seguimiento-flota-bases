# Seguimiento de Precisión — Flota (Web con Bases)

## Ejecutar local (opcional)
```bash
python -m venv .venv
. .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
streamlit run app.py
